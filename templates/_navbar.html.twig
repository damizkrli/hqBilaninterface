<!-- Navbar -->
<nav class="navbar flex items-center justify-between flex-wrap">
    <div class="flex justify-between lg:w-auto w-full pl-6 pr-2 pb-5 lg:pb-0">
        <div class="flex items-center flex-shrink-0 text-gray-800 mr-16">
            <a class="title font-semibold text-xxl tracking-tight" href="#">HQ BILAN INTERFACE</a>
{#            {{ path('branche') }}#}
        </div>
        <div class="block lg:hidden">
            <button
                    id="nav"
                    class="flex items-center px-3 py-2 border-2 rounded text-blue-700 border-blue-700 hover:text-blue-700 hover:border-blue-700">
                <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>
                        Menu</title>
                    <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/>
                </svg>
            </button>
        </div>
    </div>

    <!-- Lien vers les branches et les secteurs -->
    <div class="menu w-full lg:block flex-grow lg:flex lg:items-center lg:w-auto lg:px-3 px-8">
        <div class="text-md font-bold text-blue-700 lg:flex-grow">
            <a class="links block mt-4 lg:inline-block lg:mt-0 px-4 py-2 rounded mr-2"
               href="#">
{#                {{ path('branche') }}#}
                <i class="icon fas fa-code-branch"></i>
                Gestion des Branches
            </a>
            <a class="links block mt-4 lg:inline-block lg:mt-0 px-4 py-2 rounded mr-2"
               href="#">
{#                {{ path('secteur') }}#}
                <i class="icon fas fa-user-tie"></i>
                Gestion des Secteurs
            </a>
        </div>

        <!-- Identité de l'utilsateur connecté -->
        <div class="flex ml-10">
            <div class="flex items-center justify-center">
                <div>
                    <i class="user-icon fas fa-user"></i>
                </div>
                <div class="user-name flex flex-wrap items-center justify-center mx-2 text-center mt-8 mb-8">
                    {{ app.user ? app.user.username }}
                </div>
            </div>
        </div>
    </div>
</nav>